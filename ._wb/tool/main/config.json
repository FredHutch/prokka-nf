{
    "$schema": "https://raw.githubusercontent.com/FredHutch/bash-workbench/main/docs/schema.json",
    "name": "Prokka - Samplesheet",
    "description": "Annotate microbial genomes listed in a samplesheet (CSV)",
    "args": {
        "sample_sheet": {
            "help": "Comma-separated table with `fasta` and `name` columns",
            "wb_type": "file",
            "required": true
        }
    }
}

